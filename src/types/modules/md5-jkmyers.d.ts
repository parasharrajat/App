declare module 'md5-jkmyers' {
    /**
     * Computes the MD5 hash of the input string.
     * @param input - The string to hash.
     * @returns The MD5 hash as a hexadecimal string.
     */
    function md5(input: string): string;
    export default md5;
}
